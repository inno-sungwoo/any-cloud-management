# ============================================================
# Local profile — IDE/CLI 직접 실행 (./gradlew bootRun)
# ============================================================
# base application.properties 의 기본값이 이미 로컬 환경을 가정하므로
# 이 파일은 비워둬도 정상 동작합니다.
#
# 본인 머신 고유의 설정이 필요할 때만 아래에 추가하세요.
# 예) 다른 DB 포트, 다른 kubeconfig 경로 등.
# ============================================================

# 예시 (필요 시 주석 해제)
# spring.datasource.url=jdbc:mariadb://localhost:23306/aipaas
# kubernetes.kubeconfig.path=/Users/me/.kube/config
# anycloud.monit.default-url=http://localhost:19090
