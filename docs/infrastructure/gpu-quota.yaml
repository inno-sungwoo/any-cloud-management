# GPU 독점 방지 ResourceQuota
# ai-paas namespace에서 팀 전체 최대 GPU 수를 4개로 제한
# 초과 시 K8s가 자동 거부 → 포털에서 사용자 친화적 에러 메시지 표시 (409)
apiVersion: v1
kind: ResourceQuota
metadata:
  name: gpu-quota
  namespace: ai-pass3
spec:
  hard:
    requests.nvidia.com/gpu: "4"
    limits.nvidia.com/gpu: "4"
